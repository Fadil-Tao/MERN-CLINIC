import AuthLayout from '../Layouts/AuthLayout';
import FormLogin from '../Components/Form/FormLogin';
const LoginPage = () => {
    return (
        <AuthLayout Form={FormLogin}/>
    );
};

export default LoginPage;
